const path = require('path');const rootDir = path.resolve('.');const srcDir = path.resolve(rootDir, 'src/components/vue-cus-tabs');const outputDir = path.resolve(rootDir, 'output');const outputDistDir = path.resolve(outputDir, 'dist');const MiniCssExtractPlugin = require('mini-css-extract-plugin');module.exports = {  entry: path.resolve(srcDir, 'index.ts'),  output: {    filename: 'vue-cus-tabs.js',    path: outputDistDir  },  externals: {    vue: 'Vue'  },  module: {    rules: [      {        test: /\.tsx?$/, use: [          {            loader: 'babel-loader',          }        ]      },      {        test: /\.less$/,        use: [          {            loader: MiniCssExtractPlugin.loader, // compiles Less to CSS          },          'css-loader',          'less-loader'        ]      },    ]  },  plugins: [    new MiniCssExtractPlugin({      // Options similar to the same options in webpackOptions.output      // all options are optional      filename: 'style.css',      chunkFilename: '[id].css',      ignoreOrder: false, // Enable to remove warnings about conflicting order    }),  ],  resolve: {    // Add `.ts` and `.tsx` as a resolvable extension.    extensions: ['.ts', '.tsx', '.js','.less','.css']  },};